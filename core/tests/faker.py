"""
Faker entry point.

Use `Faker()` to create and initialize a faker generator, which can generate data by accessing properties
named after the type of data you want.
"""
from faker import Faker

faker = Faker()
